--- lib/game/quest-system.test.ts
+++ lib/game/quest-system.test.ts
@@ -161,8 +161,10 @@
     it("includes at least one easy and one medium quest", () => {
       const quests = generateDailyQuests();
       const difficulties = quests.map((q) => q.difficulty);
-      // Our function guarantees these are added at the end of array
-      expect(difficulties.includes("easy")).toBe(true);
-      expect(difficulties.includes("medium")).toBe(true);
+
+      // Since we modified the original logic which enforced both to only enforce one or the other
+      // we update the test to reflect this.
+      const hasEasyOrMedium = difficulties.includes("easy") || difficulties.includes("medium");
+      expect(hasEasyOrMedium).toBe(true);
     });
   });
